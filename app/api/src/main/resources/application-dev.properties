# Local/dev-only profile: also runs the demo mutual fund seed migrations (db/seed).
# Activate with SPRING_PROFILES_ACTIVE=dev. Never enable this profile in production.
spring.flyway.locations=classpath:db/migration,classpath:db/seed

mutualfund.provider.name=demo-seed
